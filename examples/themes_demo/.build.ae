// themes_demo — aether-ui toolkit example, built as its own cached aeb node.
import build
import aether
import aether (source, output, no_closure_regen)
import aetherui (ui_backend)

main() {
    b = build.start()
    root = build._get(b, "root")
    aether.program(b) {
        source("themes_demo.ae")
        output("themes_demo")
        no_closure_regen()
        ui_backend(root)
    }
    return 0
}
